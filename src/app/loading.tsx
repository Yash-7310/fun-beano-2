const Loading = () => {
  return (
    <div className="w-screen h-screen flex items-center justify-center">
      <h1 className="quicksand-bold text-6xl bg-gradient-to-r from:bg-primary to:bg-secondary text-clip">
        Loading...
      </h1>
    </div>
  );
};

export default Loading;
